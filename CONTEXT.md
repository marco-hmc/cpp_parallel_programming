# 并行编程 handbook — benchmark 套件术语

本术语表服务于 `1_benchmark/` 基准测试的叙述与结论：每个词对应一个精确概念，报告和代码注释必须遵守。

## Language

### 执行语义

**完成顺序**：
任务实际执行完毕的顺序，由调度与任务时长共同决定，无法预知。
_Avoid_: 实际顺序

**提交顺序**：
任务被加入队列的顺序，即 `submitTask` 的调用顺序。
_Avoid_: 入队顺序、调用顺序

**死锁**：
全体 worker 被阻塞互相等待，系统零进展的状态。在嵌套等待场景中，父任务数 ≥ worker 数且父任务阻塞等待子任务时数学必然发生。
_Avoid_: 卡死、挂起

**饥饿**：
某个任务无限期得不到执行机会。与死锁的区别：其他任务仍在推进。
_Avoid_: 慢

**并行度侵蚀**：
阻塞中的 worker 被任务占用，池的有效并行度线性下降，吞吐下降但任务终会完成。嵌套等待安全区（父任务数 < worker 数）实测到的现象即此。
_Avoid_: 饥饿（旧用法）、线程饥饿

**过度订阅**：
可运行线程数超过逻辑核数，引发上下文切换风暴。多池方案（每父任务一个子池）的代价即此。
_Avoid_: 线程爆炸、超订

### 实验设计

**配对对比**：
两个基准的唯一差异是一个设计轴，其余（池、任务、参数、reduce 执行者）完全相同。结论只能从配对差值得出。
_Avoid_: 横向比较

**控制组**：
通过构造使某变量消失的基准（如 oversized 池消掉创建开销、parallel_reduce 消掉串行阶段），用于把混合效应拆成可归因的单项。
_Avoid_: 对照组

**基线**：
量化某个前提本身成本的基准。case3 的 parallel_reduce 基线回答「Reduce 必须串行」这个前提值多少钱。

**串行占比旋钮**：
`serial_factor` 参数：串行阶段工作量 ×k 而并行阶段不变，用于控制串行/并行工作量比值。
_Avoid_: 串行工作量（旧用法——vector_size 同时缩放两个阶段，不能充当该旋钮）

### 工作负载

**Map 阶段**：
并行阶段：N 个独立任务在池上执行 `heavyComputation`。
_Avoid_: 并行阶段（当上下文已用 Map/Reduce 框架叙事时）

**Reduce 阶段**：
串行阶段：对每个 Map 结果执行 `serialComputation` 并累加。在 case3 中这是人为设定的前提（求和本身可结合），受 parallel_reduce 基线检验。
_Avoid_: 串行阶段（同 Map 的理由）

**流水线**：
Map 与 Reduce 交错执行的策略：不等全部 Map 完成即开始对已完成结果做 Reduce。与完成顺序无关——按提交顺序逐条 get 也算流水线。
_Avoid_: 按完成顺序处理（那是「完成顺序感知」，是另一个轴）
